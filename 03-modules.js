// Modules - Encapsulated Code 9only share minimum)
// CommonJS, every file is module (by default)
const names = require("./04-namesModules");
const sayHi = require("./05-utilsModules");

const data = require("./06-alternative-syntax");
// console.log(data);
require("./07-mind-grenade");

// sayHi("susan");
// sayHi(names.john);
// sayHi(names.peter);
